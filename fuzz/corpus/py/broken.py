# Deliberately broken Python — missing colons, bad indents, incomplete expressions
import os
from pathlib import

def run_command(cmd)
    result = exec(cmd)
    return result

def broken_function(data
    cleaned = data.strip(
    return cleaned.

class Incomplete:
    def __init__(self, db)
        self.db = db

    def get_user(self, user_id):
        query = f"SELECT * FROM users WHERE id = {user_id}"
        return self.db.execute(

def trailing(
    exec(
    eval(
